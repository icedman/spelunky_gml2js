function oMagmaTrail_OTHER($) {
  with ($) {
    action_kill_object();
  }
}

function oMagmaTrail_CREATE($) {
  with ($) {
    action_inherited();

    image_speed = 0.4;
  }
}

class oMagmaTrail extends oDrawnSprite {
  // variables
}
